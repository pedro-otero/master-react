import React from 'react';
import PropTypes from 'prop-types';

export const TrackItem = ({ number, fromSpotify, fromSearch }) => {
  return <div>

  </div>;
};

TrackItem.propTypes = {
  fromSearch: PropTypes.object,
  fromSpotify: PropTypes.object,
  number: PropTypes.number,
};

export default TrackItem;
