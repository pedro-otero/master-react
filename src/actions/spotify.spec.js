import { getCurrentPlayback } from './spotify';

describe('Spotify async actions', () => {
  describe('getCurrentPlayback', () => {
    //getCurrentPlayback()(jest.fn(), null, { spotifyApi, backend });
    //getCurrentPlayback()(jest.fn(), null, { spotifyApi, backend });
  });
});
